---
import Base from "../layouts/Base.astro";
import Hero from "../components/Hero.astro";
import Features from "../components/Features.astro";
import Screenshots from "../components/Screenshots.astro";
import Community from "../components/Community.astro";
import Cta from "../components/Cta.astro";
import Footer from "../components/Footer.astro";
---

<Base
	title="Actual Budget Tweaks"
	description="Customize Actual Budget's interface with themes, layouts, and visibility settings. Available for Chrome and Firefox."
>
	<Hero />
	<Features />
	<Community />
	<Screenshots />
	<Cta />
	<Footer />
</Base>

<script>
	const els = document.querySelectorAll("[data-reveal], [data-stagger]");
	if (els.length) {
		const io = new IntersectionObserver(
			(entries) => {
				entries.forEach((e) => {
					if (e.isIntersecting) {
						e.target.classList.add("visible");
						io.unobserve(e.target);
					}
				});
			},
			{ threshold: 0.1 },
		);
		els.forEach((el) => io.observe(el));
	}
</script>
